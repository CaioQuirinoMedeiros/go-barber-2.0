export type ImageDTO = {
  uri: string;
  type: string;
  name: string;
};

export type ProfileFormData = {
  name: string;
  email: string;
  password: string;
  old_password: string;
  password_confirmation: string;
};
